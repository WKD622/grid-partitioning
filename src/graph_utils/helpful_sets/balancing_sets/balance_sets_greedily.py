def balance_greedily(G, partition_a, partition_b, partitions_vertices):
    pass
    # a_partition_size = len(partitions_vertices[partition_a])
    # b_partition_size = len(partitions_vertices[partition_b])
    # partition_size_diff = abs(a_partition_size - b_partition_size)
    # if a_partition_size > b_partition_size:
    #
    # else:
    #     pass
